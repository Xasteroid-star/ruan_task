# AGENTS.md — 「一箭又一箭」项目总规则

本文件为 AI 编码助手（Claude Code 等）在本项目工作时的总规则与约定。开始任何开发任务前请先阅读。需求以本目录下的 `要求.txt` 为准，本文件是其落地约定。

## 1. 项目定位

「一箭又一箭」是一款点击式箭头解谜小游戏，为《软件工程》课程 task2 作业。

核心玩法：

- 棋盘为网格，格内放有朝向上 / 下 / 左 / 右四种方向的箭头。
- 玩家点击某个箭头：若其前进方向（同一行或同一列）到棋盘边界之间没有其他箭头阻挡，该箭头飞出棋盘并被消除；
- 若路径上有其他箭头阻挡，则不能消除，并给出明显的碰撞反馈（晃动 / 变色 / 文字等），同时消耗一次失误机会；
- 清空当前关卡全部箭头 → 通关，进入下一关；
- 失误次数耗尽 → 本关失败，可重新开始。

## 2. 技术栈与运行

- 语言：Python 3.8+
- 图形库：Pygame（首选，动画与碰撞反馈体验好；如需更换请先说明理由）
- 依赖：记录在 `requirements.txt`
- 运行：`python main.py`
- 测试：`python -m pytest tests/ -v`

## 3. 目录结构

```
task2/
├── main.py            # 入口：初始化 pygame、主循环、状态机调度
├── requirements.txt   # 依赖清单（pygame）
├── README.md          # 项目说明（按作业要求）
├── AGENTS.md          # 本文件
├── game/
│   ├── __init__.py
│   ├── config.py      # 常量：窗口尺寸、网格、颜色、字体、帧率
│   ├── models.py      # 数据模型：Direction / Arrow / Level（纯数据，无 pygame 依赖）
│   ├── pathing.py     # 核心：路径检测（纯函数，可单元测试）
│   ├── board.py       # Board：箭头布局、点击命中、消除与失误判定
│   ├── levels.py      # 关卡数据（≥ 3 关，含失误次数）
│   ├── state.py       # 游戏状态机：START / PLAYING / LEVEL_CLEAR / GAME_OVER
│   └── renderer.py    # pygame 绘制：箭头、碰撞反馈动画、HUD、按钮
├── assets/            # 可选素材（使用网络素材须注明来源）
└── tests/
    ├── test_pathing.py  # 路径检测单元测试
    └── test_levels.py   # 关卡可解性与数据校验
```

## 4. 架构设计

### 分层原则

- 游戏逻辑（数据模型、路径检测、关卡判定）与渲染（pygame 绘制）严格分离。
- 核心算法写成无副作用、无 pygame 依赖的纯函数，便于单元测试。
- 关卡用数据描述，逻辑与数据解耦，便于增改关卡。

### 核心数据模型（models.py）

- 方向 `Direction`：上 / 下 / 左 / 右（`UP` / `DOWN` / `LEFT` / `RIGHT`）。
- 箭头 `Arrow`：位置 `(row, col)` + 方向 + 状态（在盘 / 飞出中 / 已消除）。
- 关卡 `Level`：网格行列数 + 箭头列表 + 允许失误次数。
- 棋盘 `Board`：箭头集合，提供点击命中、路径检测、消除。

### 核心算法：路径检测（pathing.py）

对箭头 `a = (row, col, direction)`，判断能否飞出：

- `RIGHT`：检查同一行 `(row, col+1 .. col_max)` 是否含其他箭头；
- `LEFT`：检查同一行 `(row, 0 .. col-1)`；
- `DOWN`：检查同一列 `(row+1 .. row_max, col)`；
- `UP`：检查同一列 `(0 .. row-1, col)`。

区间内无其他箭头 → 可飞出；否则被阻挡。

### 游戏状态机（state.py）

```
START → PLAYING → LEVEL_CLEAR → (下一关) PLAYING
              ↘
            GAME_OVER（失误耗尽）
```

- `PLAYING` 内点击被阻挡箭头 → 消耗一次失误；失误耗尽 → `GAME_OVER`。
- 清空本关全部箭头 → `LEVEL_CLEAR`；最后一关之后 → 通关总界面。

## 5. 编码规范

- 模块职责单一，函数小而清晰；关键函数写中文注释说明意图。
- 命名：类用 PascalCase，函数 / 变量用 snake_case，常量全大写。
- 渲染代码只负责"画"，不做玩法判断；判断逻辑放 models / pathing / board。
- 新增功能先补测试，尤其路径检测与关卡可解性。

## 6. 测试规范

- 至少覆盖：路径检测（四方向、阻挡 / 无阻挡 / 边界）、关卡可解性、失误次数判定。
- 可用 pytest 自动化，也允许手工试玩记录（须在博客记录过程与结果）。
- 每个关卡提交前必须由本人实际试玩，确认存在合理通关顺序。

## 7. 作业合规规则（重要）

- 必须使用至少一种 AIGC 工具开发（本项目即用 Claude Code），并在博客记录至少 3 次真实 AIGC 协作过程（要求、AI 完成内容、实际效果、是否修改）。
- 不得直接复制他人完整项目；不得使用原游戏《一箭又一箭》的代码、素材、音效或关卡。
- 使用网络代码 / 图片 / 音效须在 README 或博客注明来源。
- 仓库禁止出现密码、Token、API Key 等敏感信息。
- AI 生成的代码进入项目后由提交者本人负责，须能解释关键代码。

## 8. Commit 与提交规范

- 开发过程多次有意义 Commit，禁止截止前一次性提交。
- Commit 信息用 Conventional Commits 风格，示例：

```
feat: 完成游戏棋盘和箭头显示
feat: 实现四个方向的路径检测
feat: 增加碰撞反馈和失误次数
feat: 增加关卡切换功能
fix: 修复向上检测时的数组越界
docs: 完善README和运行说明
```

## 9. README 要求

README.md 至少包含：项目名称、游戏简介、开发环境、安装与运行方法、游戏操作说明、游戏截图。
